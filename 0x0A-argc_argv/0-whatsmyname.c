#include "main.h"

/**
 * main - program that prints its name, followed by a new line
 *
 * @argc: int type parameter
 * @argv: pointer to char type parameter
 *
 * Return: 0 on success
 */

int main(int __attribute__((unused))argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
